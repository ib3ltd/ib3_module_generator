<?php

/**
 * Module {{machine_name}} Update # 8101.
 */
 /*
function {{machine_name}}_update_8101() {
  $config_factory = \Drupal::service('config.factory');

  $fields = $config_factory->get('{{machine_name}}.settings')->get('fields');
  $fields['default']['nid'] = [
    'prepareNidFromNode'
  ];

  $config_factory->getEditable('{{machine_name}}.settings')->set('fields', $fields)->save();
  drupal_set_message('Module {{machine_name}} Update # 8101 () was executed successfully.');
}
*/
